# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Env / secrets — never in git history
.env

# Private data — parquet source files stay OUT of the open-source repo.
# They live in private object storage (Cloudflare R2); see DEPLOY.md.
data/
*.parquet

# Django
staticfiles/
media/
db.sqlite3

# Tooling caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# OS / editors
.DS_Store
.idea/
.vscode/

# Agent tooling — local state, never published. `.claude/launch.json` stays
# tracked: contributors use it.
.superpowers/
.claude/worktrees/
.claude/settings.local.json
